class Receipt {
    getReceipt() {
        const menus = null;
        const totalAmount = null;
        const totalPromotionAmount = null;
        const promotionContents = null;
        const presentationMenu = null;
        const expectAmount = null;
        const eventBadge = null;
        return {
            menus,
            totalAmount,
            totalPromotionAmount,
            promotionContents,
            presentationMenu,
            expectAmount,
            eventBadge,
        };
    }
}
export default Receipt;
